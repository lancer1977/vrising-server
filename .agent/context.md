# Context: DS-vrising (Dev Studio Target)

## What is this?
This repository is managed by **Dev Studio**, a centralized AI-driven toolkit for workflow automation. It uses a structured SDLC with Orchestrator, Builder, and Validator agents.

## Workflow Status
Work moves through the `00_agile/` folders:
`planning` → `backlog` → `spec` → `doing` → `review` → `done`
