# Templates

Repo-wide templates are not currently maintained here.

Use `00_agile/templates/` for local work-card templates. Add files here only
when a repeatable V Rising runtime validation, deployment handoff,
configuration review, or release-readback template should be shared across
Docker, docs, and operator surfaces.
